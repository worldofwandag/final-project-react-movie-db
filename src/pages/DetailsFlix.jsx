import React from 'react';
import { useParams } from "react-router-dom";

const DetailsFlix = ( {movie} ) => {
    const {imdbID } = useParams
    return (
        <div>
           <h1>UNDER CONSTRUCTION</h1>
        </div>
    );
}

export default DetailsFlix;
